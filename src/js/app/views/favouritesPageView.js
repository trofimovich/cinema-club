define([
		"jquery",
		"underscore",
		"backbone",
		"app/templates/cinemaClubTmpls"
], function($, _, Backbone, cinemaClubTmpls) {
	var FavouritesPageView = Backbone.View.extend({
		
	});

	return FavouritesPageView;
});